# Test-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
# Canonical machine-schema validation of data/machines and the runtime
# topology machines (tests/test_machine_schema.py, #38).
jsonschema==4.25.1
